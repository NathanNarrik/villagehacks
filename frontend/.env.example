# CareCaller / ScribeShield API — must match the port you pass to uvicorn
# If Windows blocks port 8000 (WinError 10013), run: uvicorn ... --port 8001 and use that here.
VITE_API_URL=http://127.0.0.1:8000
