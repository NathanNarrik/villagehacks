# Benchmark Audio v1

This is the fixed benchmark audio set used for repeatable scoring.

Rules:

1. Keep `clip_id` <-> audio mapping stable.
2. Replace files only if they are corrupted.
3. Content changes require a new version folder (`v2`, `v3`, ...).

`manifest.csv` is the canonical mapping for this benchmark version.
