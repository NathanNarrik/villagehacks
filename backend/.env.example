# --- Person B (AI/NLP) keys — fill these in first ---
ANTHROPIC_API_KEY=
TAVILY_API_KEY=

# --- Person A (STT) key — only required for /transcribe end-to-end ---
ELEVENLABS_API_KEY=
# Optional legacy alias (backend accepts either env name):
# ELEVEN_LABS_API_KEY=

# --- Tunables (defaults are fine) ---
CLAUDE_MODEL=claude-sonnet-4-5
TAVILY_CALL_CAP=5
TAVILY_CACHE_TTL_SEC=3600
FRONTEND_ORIGIN=http://localhost:5173
